# 反编译速查（Elin.dll）

> 这里只收已通过反编译或实测确认的 API 与机制。源码存档见 `docs\研究\`。工具命令查 `工具与依赖.md`。

## 验证标记

- ✅反编译：由 ilspycmd 确认。
- ✅实测：游戏或旧项目确认。
- ⏳经验：尚未独立确认。
- ⚠️：容易出错的行为。

## Trait 与交互

- 自定义类必须在全局命名空间，类名为 `Trait` + Excel trait 值；启动时把当前程序集加入 `ClassCache.assemblies`。
- 常用继承链：`Trait`、`TraitContainer`、`TraitShippingChest`、`TraitCrafter`、`TraitFactory`、`TraitWorkbench`、`TraitTool`。
- 背包“使用”要求 Trait 重写 `CanUse(Chara)` 返回 `true`；执行逻辑放在 `OnUse`。
- `TraitCrafter.HoldAsDefaultInteraction` 为 `true`；便携设备要右键直接使用时重写为 `false`。
- 地图交互使用 `TrySetAct(ActPlan)`；`actUse`、`open` 等文本找不到翻译时可回退原文。

## Thing、工具与材料

- 采集判定看实例元素：220 挖矿、225 伐木、230 挖掘、286 农作、245 钓鱼、237 驯服。
- 材料的 `elementMap` 才是默认采集元素来源；自定义工具在 Trait 的 `OnCreate` 中调用 `owner.elements.SetBase(id, 1)` 最稳妥。
- 武器判定：近战类别为 `category.IsChildOf("melee")`；工具通常使用非 melee 类别并让 `offense` 留空。
- `offense` 必须为空或长度至少为 2；长度 1 可能在创建/渲染配方时越界。
- `_tileType=ObjBig` 阻挡通行，`Obj` 可跨过。
- 堆叠上限来自 `Category.maxStack`，不是 Thing 独立字段。
- `Thing.ApplyMaterial` 使用 `defMat`，已确认 `oak` 有效，`wood` 无效。

## 工作台与混合机

- 工作台 UI：`EClass.ui.AddLayer<LayerCraft>().SetFactory(thing)`。
- 工作台配方来自 Thing 行的 `factory` + `components`；Recipe 表不用于普通工作台。
- `factory` 为空表示不可制作，但 xlsx 空字符串可能回退默认值；需要覆盖默认值时使用不存在的 id（如 `none`）。
- 混合机通过 `TraitCrafter.IdSource` 匹配 Recipe.factory。
- `Recipe.ing1/ing2/ing3` 写纯物品 id，不带数量；`sp` 与 `time` 是 int。
- Recipe 的 `tag` 必须含 `known` 才会显示。
- `Resource`/`FixedResource` 产生物品；`Grind` 不产生普通产物。
- `TraitCrafter.GetSource` 非 virtual；自定义匹配需 Harmony Prefix 接管。
- `LayerCraft.windowList.AddTab` 可添加分类；隐藏默认分类可反射设置 `tabBuilt=true`。

## 容器、UI 与队伍

- `LayerInventory.CreateContainer(thing)` 打开容器。
- 容器大小可由 Trait 参数 `GetParam(1)`、`GetParam(2)` 设置。
- 背包容器继承 `TraitContainer` 并启用 `CanOpenContainer` 即可获得打开交互；自定义容器应覆写空的 `Prespawn`，防止随机内容。
- 出货箱基类为 `TraitShippingChest`；大出货箱 id 需先检查源表，不存在时回退 `container_shipping`。
- 通用选择列表：`EClass.ui.AddLayer<LayerList>().SetList(...)`；`autoClose:false` 可重复刷新。
- 自定义 Layer 需要游戏 prefab，Mod 不应假设能够新建窗口；优先组合现有 Layer。
- 消耗背包物品：`EClass.pc.things.Find(id)` 后 `ModNum(-n)`；队友列表为 `EClass.pc.party.members`，不含玩家自身。

## 技能、属性与 Buff

- `elements.ModBase` 直接改等级/属性；`ModExp` 加经验；`HasBase` 判断是否已学；`Learn` 学习。
- 主属性 id：力量 70、体质 71、灵巧 72、感知 73、学习 74、意志 75、魔力 76、魅力 77。
- `tempElements` 初始可能为 null；普通临时增益用 `Chara.ModTempElement`。该方法有上限，突破时需初始化容器后直接 `ModBase`。
- 临时增益由游戏周期衰减；永久属性使用 `chara.elements.ModBase`。

## 自动化、电力与放置

- 地图物品周期钩子为 `Trait.OnSimulateHour(VirtualDate)`；没有可靠的分钟级物品钩子。
- 相邻格：`point.ForeachNeighbor(..., diagonal:false)`、`point.FindThing<T>()`、`point.Things`。
- `目标容器.AddCard(t)` 会自动从原容器或地面摘除物品。
- Thing 的 `electricity` 正数发电、负数耗电；缺电可检查 `EClass._zone.electricity < 0`。
- 开关状态为 `owner.isOn`，切换使用 Trait 的 `Toggle`。
- 放置回调：`Trait.OnChangePlaceState`；肥料类需使用本体可放置类别，如 `fertilizer`。
- 作物催熟：`point.growth.SetStage(stages.Length - 1)`。

## 鉴定、施法与状态

- 普通鉴定：`ActEffect.Proc(EffectId.Identify, caster, target)`；高级鉴定用 `GreaterIdentify`；去诅咒用 `Uncurse`。
- `Thing.IsIdentified` 判断完全鉴定；神话及以上稀有度通常需要高级鉴定。
- 施法：`pc.UseAbility(elementId, target)`；法术/技能书的 id 通常存于 `Thing.refVal`。
- `Trait.OnTickHeld` 只对热键栏当前选中物品触发；背包全局生效需补丁 `Chara.TickConditions`。
- Harmony Prefix 挂 `Chara.Die` 并返回 `false` 可取消死亡；`ConInvulnerable.value` 表示剩余回合。
- 自定义 Condition 的公共持久化字段使用 `[JsonProperty]`，随角色存档保存。

## 数据覆盖与持久化

- xlsx 写入已有 Thing id 会整行替换，空列使用本表默认值，可能破坏官方物品。
- 给已有物品加配方，推荐在 `SourceManager.Init` Postfix 中只改目标源行的 `factory`、`components`、`recipeKey`。
- Card 没有安全的自由字段可用于 Mod 设置；按 uid 使用独立 CSV，并在 `Game.Save` Postfix 写盘。角色绑定数据优先使用自定义 Condition。

## Mod Options

- 自动绑定要求配置对象中的 `ConfigEntry<T>` 为实例字段，并使用 `[ModCfgToggle]`、`[ModCfgSlider]` 或 `[ModCfgDropdown]`。
- Mod Options 是软依赖；注册用 try-catch，缺失时仍保留 BepInEx cfg 配置。
- 手动布局使用 `SetPreBuildWithXml`、`OnBuildUI` 和 `GetPreBuild<Opt...>`。
- 手动滑块需在 `OnBuildUI` 设置 `Step`；标题中的当前值需自行刷新。
