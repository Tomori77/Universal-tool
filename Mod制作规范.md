# Mod 制作规范（项目总纲）

> 流程与发布规则的唯一总纲。API 查 `反编译速查.md`，工具与路径查 `工具与依赖.md`。当前基线：Elin EA 0.23.x、BepInEx 6.0.0-pre.1。

## 1. 工作原则

- 编码前明确假设；有歧义先询问。
- 用最简单、最少代码的方案。
- 只修改任务直接相关内容，不顺手重构。
- 先定义成功标准；修复问题先复现，新增功能覆盖异常输入。
- 多步骤任务先写计划，并为每步指定验证方式。

## 2. Mod 类型

| 类型 | 适用内容 | DLL |
|---|---|---|
| 数据表 Mod | 物品、配方、技能等纯数据 | 通常不需要 |
| 插件 Mod | 代码改变玩法 | 需要 |
| 混合 Mod | 数据表 + 自定义 Trait | 需要 |
| 资源 Mod | 贴图、音效、立绘 | 不需要 |
| 语言包 | 文本替换 | 不需要 |
| 设置菜单 | Mod Options 页面 | 可选依赖 |

## 3. 标准流程

1. 创建 `<Mod名>设计规范.md`，记录需求、决策、步骤和验证方式。
2. 确认物品、机制、语言、设置和资源需求。
3. 输出执行计划和完成标准。
4. 实现工程、源码、数据表、贴图、本地化和设置页面。
5. 删除旧的 `docs\待转移\<Mod名>\`，构建并整理发布文件。
6. 按验证链检查，全部通过后才算完成。

## 4. 目录与发布包

开发目录可包含工程、`Script\`、`refs\`、`LangMod\`、`Texture\`、`package.xml` 和设计文档。

发布目录 `docs\待转移\<Mod名>\` 只放 DLL（如有）、`package.xml` 以及按需的 `LangMod\`、`Texture\`、`preview.jpg`、`ModOptions.xml`。不要放源码、工程、`obj\`、`refs\`、`*.pdb` 或设计文档。每次更新先删除整个目标 Mod 文件夹，再根据当前编译结果重新创建完整发布包，禁止在旧目录上增量覆盖。发布包由用户复制到游戏 `Package\`。

## 5. 关键规则

- 插件目标框架为 `net472`，游戏引用设 `<Private>false>`。
- 自定义 Trait 的类名为 `Trait` + Excel trait 值，放在全局命名空间，并在启动时注册程序集。
- xlsx 放在 `LangMod\<语言>\`，使用三行头，生成必须用 `xlsxwriter`。
- 工作台配方使用 Thing 行的 `factory` + `components`；混合机才使用 Recipe 表。
- `offense` 必须为空或长度至少为 2；工具通常留空。
- 不要用 xlsx 整行覆盖已有官方物品；修改已有物品时使用运行时补丁。
- 构建前删除旧输出，避免残留文件干扰加载。

## 6. 完成标准

1. 编译无错误。
2. `BepInEx\LogOutput.log` 有插件加载记录，不能是 `0 plugins to load`。
3. 有自定义 Trait 时，`Player.log` 有类型注册日志。
4. `Player.log` 有 `#mod-content added` 源表导入记录。
5. 游戏内物品可制造、放置或使用，贴图和 Trait 正常。
6. 配方重建无 `Failed to create recipe`。
7. 设置页面正常，配置写入 `BepInEx\config\`（如有）。
