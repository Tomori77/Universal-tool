# 工具与依赖

> 本文只记录 Elin Mod 的开发环境、路径、依赖和常用命令。流程查 `Mod制作规范.md`，API 查 `反编译速查.md`。
> 默认基线：Elin EA 0.23.x、BepInEx 6.0.0-pre.1。

## 1. 工具链

| 工具 | 用途 | 要点 |
|---|---|---|
| .NET SDK | 编译 DLL | 目标 `net472`，`LangVersion=latest`，使用 `dotnet build` |
| BepInEx 6.0.0-pre.1 | 插件框架 | 游戏自带 |
| ilspycmd 9.1.0 | 反编译 | `-l` 要带类型参数，如 `-l c`；输出转 UTF-8 |
| Python 3 + xlsxwriter | 生成 xlsx | 必须使用 xlsxwriter |
| Python 3 + openpyxl | 读回校验、读取官方 xlsx | 不用于生成 Mod xlsx |
| PowerShell 5.1 | 构建、打包、贴图 | 脚本使用 ASCII，读取 UTF-8 文本指定编码 |
| `tools\适配贴图.ps1` | 转换贴图 | `-Mode texture` 转 32×32 PNG；`-Mode cover -Size 512` 转封面 |

## 2. 常用命令

### 反编译

```powershell
ilspycmd "D:\Steam\steamapps\common\Elin\Elin_Data\Managed\Elin.dll" -t TraitCrafter -r "D:\Steam\steamapps\common\Elin\Elin_Data\Managed" | Out-File TraitCrafter.cs -Encoding utf8
ilspycmd Elin.dll -l c > types.txt
```

### 构建和打包

```powershell
$src = "<Mod名>"
$dst = "docs\待转移\<Mod名>"
Remove-Item $dst -Recurse -Force
dotnet build "$src\$src.csproj" -c Release -o $dst
Copy-Item "$src\LangMod" $dst -Recurse -ErrorAction SilentlyContinue
Copy-Item "$src\Texture" $dst -Recurse -ErrorAction SilentlyContinue
Copy-Item "$src\package.xml", "$src\preview.jpg" $dst -ErrorAction SilentlyContinue
Remove-Item "$dst\*.pdb" -ErrorAction SilentlyContinue
```

发布包不要包含源码、工程、`obj`、`refs` 或 `pdb`。用户负责把 `docs\待转移\<Mod名>\` 复制到游戏 `Package\`。

待转移包更新规则：先删除整个 `docs\待转移\<Mod名>\` 目标 Mod 文件夹，再根据当前 Release 编译结果重新创建并放入完整发布文件。不得在旧目录上增量覆盖，避免残留文件进入测试包。标准内容包括编译得到的 DLL、`package.xml`、`preview.jpg`、需要的 `LangMod\`、`Texture\` 和其他发布必需文件；不放 `*.pdb`。

### 自动设备时间调度

- 统一调度器位于 `Universal tool\\Script\\TraitAutomationScheduler.cs`。
- 实时模式通过 `GameDate.AdvanceMin` 监听时间推进，每跨过一个 10 分钟节点执行一次 Tick；每小时最多执行 6 次。
- 离线模式通过 `Zone.OnSimulateHour` 在每小时模拟时补算 6 次，使用与实时模式相同的单次 Tick 入口。
- 当前接入统一调度器的设备为自动凝华器、自动复制机、自动生成器、物品收集器、输入终端、流体抽取器、自动农业机、矿物生成机和自动熔炉。
- 所有上述设备均由统一调度器每 10 分钟调用一次；设备 Trait 只实现单次 Tick 业务逻辑。
- 新增需要每 10 分钟工作的设备时，只应实现单次 Tick 业务并接入统一调度器，不得在 Trait 内重复实现独立时间循环。

### 贴图

```powershell
.\tools\适配贴图.ps1 -Src item.jpg -Out item.png -Mode texture
.\tools\适配贴图.ps1 -Src cover.jpg -Out preview.jpg -Mode cover -Size 512
```

物品贴图使用 32×32、透明背景、俯视视角；文件名必须与物品 id 一致。

## 3. 图片工作流

### 通用目录约定

Mod 的图片工作区使用以下目录：

```text
<Mod目录>\图片\
├── 未修改图片\   # 用户提供的原始图片
├── 使用图片\     # 已确认使用的最终图片
└── 归档\         # 已使用过的原始图片
```

### 操作规则

1. 原始图片由用户放入 `<Mod目录>\图片\未修改图片\`；AI 不代替用户生成或放入原始图片。
2. 发现新原始图片后，先询问用户是否需要处理；未经用户确认不得修改图片。
3. 用户确认需要处理时，使用贴图脚本去背景、裁剪和缩放，处理结果保存到 `图片\使用图片\`。
4. 用户确认不需要处理时，不修改图片，直接复制到 `图片\使用图片\`。
5. 图片文件名使用物品 id，例如 `ut_auto_farming.png`。
6. 确认图片已被使用后，将原始图片从 `图片\未修改图片\` 移到 `图片\归档\`。
7. 只有 `Texture\` 中的最终图片会进入发布包；`图片\` 下三个工作目录不打包。

### 贴图命令

```powershell
.\tools\适配贴图.ps1 -Src item.jpg -Out item.png -Mode texture
.\tools\适配贴图.ps1 -Src cover.jpg -Out preview.jpg -Mode cover -Size 512
```

物品贴图使用 32×32、透明背景、俯视视角；封面使用 512×512 JPG。确认 `图片\使用图片\` 中的图片后，再复制到 `Texture\`。

## 4. 默认路径

| 项目 | 路径 |
|---|---|
| 游戏根目录 | `D:\Steam\steamapps\common\Elin\` |
| 游戏程序集 | `Elin_Data\Managed\` |
| BepInEx 核心 | `BepInEx\core\` |
| Mod 安装目录 | `<游戏根>\Package\<Mod文件夹>\` |
| 插件日志 | `BepInEx\LogOutput.log` |
| Unity 日志 | `C:\Users\<用户>\AppData\LocalLow\Lafrontier\Elin\Player.log` |
| 配置文件 | `BepInEx\config\<作者>.<模块名>.cfg` |
| 反编译源码 | `docs\研究\`、`docs\研究\Elin_full\` |
| 参考模组 | `docs\工坊MOD参考\` |
| 官方可读数据 | `Package\_Lang_Chinese\Lang\CN\Game\Thing.xlsx`、`Category.xlsx` |

AI 不直接修改游戏安装目录，也不代替用户启动游戏验证。

## 5. csproj 引用

所有游戏和第三方引用应设 `<Private>false>`，避免复制进发布包。

- `Elin.dll`、`Plugins.BaseCore.dll`、`Plugins.Modding.dll`、`Plugins.UI.dll`、`Plugins.Scripting.dll`、`Plugins.Sound.dll`、`Plugins.ActorSystem.dll`
- `UnityEngine.dll`、`UnityEngine.CoreModule.dll`、`UnityEngine.UI.dll`
- `BepInEx.Core.dll`、`BepInEx.Unity.dll`、`0Harmony.dll`
- `Newtonsoft.Json.dll`（自定义 Condition 持久化时）
- `docs\refs\ModOptions.dll`（需要设置菜单时，与工程目录相对）

NuGet 不需要外部包；离线还原出现 `NU1900` 通常不影响本项目。

## 6. 运行期依赖

- Harmony 随 BepInEx 提供，无需单独安装。
- Mod Options 来自工坊 3381182341，GUID 为 `evilmask.elinplugins.modoptions`。注册必须软降级：使用 try-catch，缺少时保留 cfg 配置。

## 7. 编码约定

- 源码、JSON、Markdown 使用 UTF-8。
- PowerShell 脚本尽量只使用 ASCII，避免中文和特殊符号被按 GBK 误读。
- ilspycmd 输出通过 `Out-File -Encoding utf8` 保存。
- xlsx 生成用 xlsxwriter，生成后可用 openpyxl 读回检查。

## 8. 日志排错

| 问题 | 搜索位置和关键词 |
|---|---|
| 插件加载或 BepInEx 报错 | `LogOutput.log`；搜索 `Loading`、`0 plugins to load` |
| Mod 的 `Debug.Log` | `Player.log` |
| 源表导入 | `Player.log`；搜索 `#source`、`#mod-content` |
| 配方重建 | `Player.log`；搜索 `Rebuilding recipe list`、`Failed to create recipe` |
| 配置写入 | `BepInEx\config\` |

## 9. 版本号

- `package.xml` 的 `version` 使用游戏版本号，例如 `0.23.90`。
- Mod 自身版本写在 `[BepInPlugin]` 特性中。
